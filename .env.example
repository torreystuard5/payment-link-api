# .env.example

# Random hex string for JWT signing (generate with: openssl rand -hex 32)
SECRET_KEY=change_me_to_random_hex

# Stripe test secret key (for Checkout in test mode)
STRIPE_SECRET_KEY=sk_test_your_test_key_here

# Database URL (optional, defaults to local SQLite file if omitted)
DATABASE_URL=sqlite+aiosqlite:///./payment-links.db

